selection name="develop" summary="Development tooling" description="Install software for packaging and development." {
    depends {
        base
    }
    packages {
        "binary(boulder)"
        "binary(git)"
        "binary(yq)"
    }
}
